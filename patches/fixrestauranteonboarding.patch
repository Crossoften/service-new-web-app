diff --git a/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.html b/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.html
index 7de1364..b860721 100644
--- a/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.html
+++ b/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.html
@@ -12,53 +12,57 @@
 
   <div class="rest-content">
 
-    @if (carregando) {
-      <p class="rest-msg">Carregando…</p>
-    } @else {
-      <div class="rest-form">
+    <div class="rest-form">
 
-        <div class="rest-field">
-          <label>Nome</label>
-          <input type="text" placeholder="Nome do restaurante" [(ngModel)]="nome" />
-        </div>
+      @if (!modoEdicao) {
+        <p class="rest-hint">Cadastre seu restaurante para começar a montar o cardápio e receber pedidos.</p>
+      }
+      @if (carregando) {
+        <p class="rest-msg">Carregando dados…</p>
+      }
 
-        <div class="rest-field">
-          <label>Descrição</label>
-          <textarea rows="3" placeholder="Descrição" [(ngModel)]="descricao"></textarea>
-        </div>
+      <div class="rest-field">
+        <label>Nome</label>
+        <input type="text" placeholder="Nome do restaurante" [(ngModel)]="nome" />
+      </div>
 
-        <div class="rest-field">
-          <label>Categoria</label>
-          <div class="rest-select" (click)="toggleCategoria()">
-            <span>{{ categoriaLabel }}</span>
-            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
-              [style.transform]="categoriaAberta ? 'rotate(180deg)' : 'rotate(0)'">
-              <polyline points="6 9 12 15 18 9"/>
-            </svg>
-          </div>
-          @if (categoriaAberta) {
-            <div class="rest-opcoes">
-              @for (cat of categorias; track cat.id) {
-                <div class="rest-opcao" [class.rest-opcao--ativo]="categoriaId === cat.id" (click)="selecionarCategoria(cat)">
-                  {{ cat.name }}
-                </div>
-              }
-            </div>
-          }
-        </div>
+      <div class="rest-field">
+        <label>Descrição</label>
+        <textarea rows="3" placeholder="Descrição" [(ngModel)]="descricao"></textarea>
+      </div>
 
-        @if (modoEdicao) {
-          <div class="rest-toggle" (click)="toggleAberto()">
-            <span>Loja {{ aberto ? 'aberta' : 'fechada' }}</span>
-            <div class="rest-toggle__switch" [class.rest-toggle__switch--on]="aberto">
-              <div class="rest-toggle__bola"></div>
-            </div>
+      <div class="rest-field">
+        <label>Categoria</label>
+        <div class="rest-select" (click)="toggleCategoria()">
+          <span>{{ categoriaLabel }}</span>
+          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
+            [style.transform]="categoriaAberta ? 'rotate(180deg)' : 'rotate(0)'">
+            <polyline points="6 9 12 15 18 9"/>
+          </svg>
+        </div>
+        @if (categoriaAberta) {
+          <div class="rest-opcoes">
+            @for (cat of categorias; track cat.id) {
+              <div class="rest-opcao" [class.rest-opcao--ativo]="categoriaId === cat.id" (click)="selecionarCategoria(cat)">
+                {{ cat.name }}
+              </div>
+            }
           </div>
         }
-
-        @if (erro) { <span class="rest-erro">{{ erro }}</span> }
       </div>
-    }
+
+      @if (modoEdicao) {
+        <div class="rest-toggle" (click)="toggleAberto()">
+          <span>Loja {{ aberto ? 'aberta' : 'fechada' }}</span>
+          <div class="rest-toggle__switch" [class.rest-toggle__switch--on]="aberto">
+            <div class="rest-toggle__bola"></div>
+          </div>
+        </div>
+      }
+
+      @if (erro) { <span class="rest-erro">{{ erro }}</span> }
+      @if (aviso) { <span class="rest-aviso">{{ aviso }}</span> }
+    </div>
 
   </div>
 
@@ -66,6 +70,9 @@
     <button class="rest-btn" (click)="salvar()" [disabled]="salvando">
       {{ salvando ? 'SALVANDO…' : (modoEdicao ? 'SALVAR' : 'CRIAR RESTAURANTE') }}
     </button>
+    @if (modoEdicao) {
+      <button class="rest-btn rest-btn--outline" (click)="irParaCardapio()">IR PARA O CARDÁPIO</button>
+    }
   </div>
 
 </div>
diff --git a/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.scss b/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.scss
index 5023f85..fc20c62 100644
--- a/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.scss
+++ b/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.scss
@@ -176,3 +176,22 @@
   &:active { opacity: 0.85; }
   &:disabled { opacity: 0.6; cursor: default; }
 }
+
+.rest-hint {
+  font-size: 13px;
+  color: var(--color-gray-600);
+  margin: 0 0 4px;
+  line-height: 1.4;
+}
+
+.rest-aviso {
+  font-size: 12px;
+  color: #16a34a;
+}
+
+.rest-btn--outline {
+  background: transparent;
+  color: var(--color-primary);
+  border: 2px solid var(--color-primary);
+  margin-top: 10px;
+}
diff --git a/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.ts b/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.ts
index 259deb3..f3a9086 100644
--- a/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.ts
+++ b/src/app/features/fornecedor/restaurante-fornecedor/restaurante-fornecedor.ts
@@ -34,6 +34,7 @@ export class RestauranteFornecedorComponent implements OnInit {
   carregando = false;
   salvando = false;
   erro = '';
+  aviso = '';
 
   get modoEdicao(): boolean {
     return this.restaurante !== null;
@@ -48,6 +49,9 @@ export class RestauranteFornecedorComponent implements OnInit {
           this.categoriaId = this.categorias[0].id;
         }
       },
+      error: (err: ApiError) => {
+        this.erro = err?.message?.trim() ? err.message : 'Não foi possível carregar as categorias.';
+      },
     });
     this.fornecedorService.meuRestaurante().subscribe({
       next: (r) => {
@@ -85,12 +89,13 @@ export class RestauranteFornecedorComponent implements OnInit {
 
   salvar() {
     this.erro = '';
+    this.aviso = '';
     if (!this.nome.trim()) {
       this.erro = 'Informe o nome do restaurante.';
       return;
     }
     if (this.categoriaId == null) {
-      this.erro = 'Selecione uma categoria.';
+      this.erro = this.categorias.length ? 'Selecione uma categoria.' : 'Não foi possível carregar as categorias. Tente novamente.';
       return;
     }
     this.salvando = true;
@@ -103,7 +108,14 @@ export class RestauranteFornecedorComponent implements OnInit {
           categoryId: this.categoriaId,
           isOpen: this.aberto,
         })
-        .subscribe({ next: () => this.concluir(), error: (e: ApiError) => this.falhar(e) });
+        .subscribe({
+          next: (r) => {
+            this.salvando = false;
+            this.aplicar(r);
+            this.aviso = 'Restaurante atualizado.';
+          },
+          error: (e: ApiError) => this.falhar(e),
+        });
     } else {
       this.fornecedorService
         .criarRestaurante({
@@ -111,20 +123,27 @@ export class RestauranteFornecedorComponent implements OnInit {
           categoryId: this.categoriaId,
           description: this.descricao.trim() || undefined,
         })
-        .subscribe({ next: () => this.concluir(), error: (e: ApiError) => this.falhar(e) });
+        .subscribe({
+          next: (res) => {
+            this.salvando = false;
+            // Usa o restaurante retornado pelo POST — não depende do /me (que pode demorar a refletir).
+            if (res?.restaurant) this.aplicar(res.restaurant);
+            this.aviso = 'Restaurante criado com sucesso!';
+          },
+          error: (e: ApiError) => this.falhar(e),
+        });
     }
   }
 
-  private concluir() {
-    this.salvando = false;
-    this.router.navigate(['/fornecedor/cardapio']);
-  }
-
   private falhar(err: ApiError) {
     this.salvando = false;
     this.erro = err?.message?.trim() ? err.message : 'Não foi possível salvar o restaurante.';
   }
 
+  irParaCardapio() {
+    this.router.navigate(['/fornecedor/cardapio']);
+  }
+
   voltar() {
     history.back();
   }
